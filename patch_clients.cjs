const fs = require('fs'); let code = fs.readFileSync('src/App.jsx', 'utf8'); code = code.replace('<div className="fixed inset-0 z-[60] flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm" onClick={() => setActiveOrderDetails(null)}>', '<div className="fixed inset-0 z-[60] p-4 bg-black/80 backdrop-blur-sm" style={{ display: "flex", alignItems: "center", justifyContent: "center", minHeight: "100vh" }} onClick={() => setActiveOrderDetails(null)}>'); fs.writeFileSync('src/App.jsx', code);