const fs = require('fs'); let code = fs.readFileSync('src/App.jsx', 'utf8'); code = code.replace(/let points = 0;\\s*if \\(row\\.cutter\\) points\\+\\+; if \\(row\\.cutDone\\) points\\+\\+;\\s*if \\(row\\.tailor\\) points\\+\\+; if \\(row\\.tailorDone\\) points\\+\\+;\\s*if \\(row\\.prep\\) points\\+\\+; if \\(row\\.prepDone\\) points\\+\\+;\\s*if \\(row\\.assembler\\) points\\+\\+; if \\(row\\.assembleDone\\) points\\+\\+;\\s*const pct = Math\\.round\\(\\(points \\/ 8\\) \\* 100\\);/, 'let completedStages = 0;\\n  if (row.cutDone) completedStages++;\\n  if (row.tailorDone) completedStages++;\\n  if (row.prepDone) completedStages++;\\n  if (row.assembleDone) completedStages++;\\n  const pct = Math.round((completedStages / 4) * 100);'); code = code.replace('const strokeColor = pct === 100 ? "#10b981" : pct >= 75 ? "#84cc16" : pct >= 50 ? "#eab308" : "#f97316";', 'const strokeColor = row.done ? "#10b981" : (pct >= 75 ? "#84cc16" : pct >= 50 ? "#eab308" : "#f97316");'); fs.writeFileSync('src/App.jsx', code);